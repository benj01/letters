{
  "name": "letters-simulation",
  "version": "1.0.0",
  "description": "Soft body letter simulation using TypeScript and Canvas",
  "private": true,
  "scripts": {
    "generate-shapes": "ts-node ./scripts/generateShapes.ts",
    "build": "npm run generate-shapes && tsc",
    "start": "http-server . -p 8080 -c-1"
  },
  "dependencies": {
    "simplify-js": "^1.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/opentype.js": "^1.3.8",
    "http-server": "^14.1.1",
    "opentype.js": "^1.3.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}